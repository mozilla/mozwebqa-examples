# The contents of this file are accessible as a dictionary via the pytest-mozwebqa plugin.
#
# Example:
# username = mozwebqa.credentials.get('username')
# password = mozwebqa.credentials.get('password')

username: admin
password: default
